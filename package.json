{
  "name": "dummy-data-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first CSV/TXT dummy data generator with a unified delimited-input core",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve-preview.mjs",
    "audit:offline": "node scripts/audit-offline.mjs",
    "measure:bundle": "node scripts/measure-bundle.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "papaparse": "5.5.4"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
